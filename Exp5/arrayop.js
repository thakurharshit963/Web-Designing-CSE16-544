// Step 1: Create an empty array to solve all employee objects
let employees = [];

// Step 2: Function to read input field and save the data 
function addEmployee() {
    //Create a single employee object from the input fields 
    let emp = {
        name:document
    }
}